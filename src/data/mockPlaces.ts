import type { PlaceWithStats } from '../context/placesContext';

/** Datos de ejemplo desactivados: la app usa Supabase. Mantener el archivo por compatibilidad con seeds documentados. */
export const MOCK_PLACES: PlaceWithStats[] = [];
